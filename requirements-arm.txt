-r requirements-common.txt

# TensorFlow Lite for ARM architectures
# Use a more compatible version for ARM devices
h5py>=3.1.0
# tflite-runtime is the optimized version of TensorFlow for ARM
tflite-runtime>=2.14.0
